---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Contact — Celestial Tarot">
  <section class="page-header">
    <div class="container">
      <h1>Contact Us</h1>
      <p>We'd love to hear from you. Reach out with questions, collaboration ideas, or just to say hello.</p>
    </div>
  </section>
  <section class="container">
    <div class="contact-grid">
      <form class="contact-form" onsubmit="event.preventDefault(); alert('Message sent! We will get back to you soon. ✦')">
        <input type="text" placeholder="Your Name" required />
        <input type="email" placeholder="Your Email" required />
        <textarea placeholder="Your Message" rows="6" required></textarea>
        <button type="submit" class="cta-btn">Send Message ✦</button>
      </form>
      <div class="contact-info">
        <h3>Find Us Online</h3>
        <a href="https://youtube.com/@Celestia1Tarot" target="_blank">▶ YouTube</a>
        <a href="https://twitter.com/Celestia1Tarot" target="_blank">𝕏 Twitter</a>
        <a href="https://tiktok.com/@Celestia1Tarot" target="_blank">♪ TikTok</a>
        <h3>Email</h3>
        <a href="mailto:hello@celestialtarot.com">hello@celestialtarot.com</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header { text-align: center; padding: 6rem 0 3rem; }
  .page-header p { color: var(--text-muted); margin-top: 0.5rem; }
  .contact-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 3rem;
    max-width: 900px;
    padding-bottom: 2rem;
  }
  .contact-form { display: flex; flex-direction: column; gap: 1rem; }
  .contact-form input, .contact-form textarea {
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    color: var(--text);
    padding: 0.8rem 1rem;
    border-radius: 8px;
    font-family: inherit;
    font-size: 1rem;
  }
  .contact-form input:focus, .contact-form textarea:focus { outline: none; border-color: var(--accent); }
  .contact-form textarea { resize: vertical; }
  .cta-btn {
    display: inline-block;
    background: linear-gradient(135deg, var(--bg-secondary), #2a2a6e);
    color: var(--accent);
    padding: 0.8rem 2rem;
    border-radius: 50px;
    border: 1px solid var(--accent);
    font-size: 1rem;
    cursor: pointer;
    font-family: inherit;
    align-self: flex-start;
  }
  .cta-btn:hover { background: var(--accent); color: var(--bg-primary); }
  .contact-info { display: flex; flex-direction: column; gap: 0.8rem; }
  .contact-info h3 { color: var(--accent); margin-top: 1rem; font-size: 1.2rem; }
  .contact-info a { color: var(--text-muted); }

  @media (max-width: 768px) {
    .contact-grid { grid-template-columns: 1fr; }
  }
</style>
